// The page the user lands on after opening the app and without a session
export const FirstRunPage = 'TabsPage';

// The main page the user will see as they use the app over a long period of time.
// Change this if not using tabs
export const MainPage = 'ContentPage';

// The initial root pages for our tabs (remove if not using tabs)
export const Tab1Root = 'MenuPage';
export const Tab2Root = 'SocialPage';
export const Tab3Root = 'CalendarPage';
export const Tab4Root = 'AlbumPage';
export const Tab5Root = 'SearchPage';
export const Tab6Root = 'SettingsPage';
